# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environments
.venv/
venv/
ENV/
env/
python_env/

# Environment variables / Secrets
.env
.venv
env.bak
venv.bak

# IDEs / Editors
.vscode/
.idea/
*.swp
*.swo

# Testing / Coverage
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
htmlcov/

# OS generated files
.DS_Store
Thumbs.db

# Rust / Cargo
target/
# Cargo.lock is committed for binary crates (scrub-rs is an app, not a lib)
*.o
*.so
*.a

# Virtual disk images (loop-device backing stores, multi-GB build artifacts)
d_d1
d_d2
d_d3
d_d4
d_p
d_q

# Sibling source trees (tracked separately / not part of this repo's scope)
szfs/
zfs/
grub-zfs/

# Generated test artifacts (regenerated by tests/integration/btrfs_test_matrix.sh / run_matrix.sh)
scrub-rs/btrfs_test_images/
scrub-rs/tests/integration/btrfs_test_images/
/root/btrfs_live_scrub_test_*/

# unRAID plugin: built binaries + bundle (produced by CI / pkg_build.sh)
plugin/source/rotshield/usr/local/emhttp/plugins/rotshield/bin/scrub-rs
plugin/source/rotshield/usr/local/emhttp/plugins/rotshield/bin/craft-corrupt
plugin/archive/
# keep the .gitkeep so the bin/ dir is tracked
!plugin/source/rotshield/usr/local/emhttp/plugins/rotshield/bin/.gitkeep

# JS tooling (pi / bun) — stray artifacts from running `pi install` or `bun` in the repo root
node_modules/
package.json
bun.lock
bun.lockb
# pi / pi-subagents runtime working data
.pi-subagents/
.pi/
